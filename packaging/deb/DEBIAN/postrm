#!/bin/sh
set -eu

case "${1:-}" in
    remove|purge)
        for home in /home/* /root; do
            if [ -d "$home" ]; then
                rm -rf -- "$home/.local/state/Display X Studio"
            fi
        done
        ;;
esac

exit 0
